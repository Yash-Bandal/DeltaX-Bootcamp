## Questions

1. Difference between `First` and `FirstOrDefault`
- First of all, `First` returns the first element that matches the condition, like where
    - If nothing found, `First` returns exception
- If we use `FirstOrDefault`, it returns `null`, therefore application is not breaked 
