# Intermediate CSharp

---
> [!Tip]
> 1. You will learn the right way to use Inheritance, and the methods of `Composition`
> 2. How to use polymorphism, extend the behaviour of program  by creating neat classees, instead of rewriting
> 3. What actually are `interfaces`
>     - Interfaces arent actually used to implement `Multiple Inheritance`
>     - They are used for increasing `Testibility` (eg.unit testing)  and `Extensibility` of your application
---
